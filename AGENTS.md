# AGENTS.md — KulinerLumajang.id Root

> **Status:** IMMUTABLE — Artefak branch `dev` saja, TIDAK masuk `main`.

**AI Agent yang kerja di project ini:**
1. Baca `ai-rules/AGENTS.md` — kontrak kerja lengkap
2. Baca `ai-rules/README.md` — mapping template → output
3. Follow semua aturan di `ai-rules/`

**Project:** KulinerLumajang.id — UMKM kuliner directory Lumajang
**Stack:** Astro + Laravel/Filament + Supabase + Xendit
**Git:** `.git/` di `apps/`, bukan di root
**Branch:** AI kerja di `dev` atau `feat/*`, bukan `main`
